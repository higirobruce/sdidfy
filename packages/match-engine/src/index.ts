// PLACEHOLDER — the real engine (mock 1:1 matcher, PAD evaluation, zeroize
// discipline, tests) is implemented per spec 03/06/07 §1.
import type { MatchEngine } from '@sdid/shared';

export function createMatchEngine(): MatchEngine {
  throw new Error('match-engine not yet implemented');
}
